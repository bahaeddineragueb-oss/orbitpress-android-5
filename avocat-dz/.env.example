# ── قاعدة البيانات — اختر واحدة ──

# للـ PC (Offline) — SQLite
DATABASE_URL="file:./prisma/maktabi.db"

# للـ Web/Production — PostgreSQL (Supabase/Neon/Docker)
# DATABASE_URL="postgresql://maktabi:maktabi123@localhost:5432/maktabi"
# DATABASE_URL="postgresql://user:pass@host:5432/maktabi?sslmode=require"

# Next
NEXT_PUBLIC_APP_NAME="مكتبي"
