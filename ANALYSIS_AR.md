# تحليل OrbitPress 4.0.0

## النطاق والمعمارية

المشروع هو مصدر تطبيق Android هجين يستخدم Kotlin مع واجهة HTML/CSS/JavaScript داخل `WebView`. نقطة الدخول هي `MainActivity.kt`، وتنفذ `NativeBridge` العمليات غير المتزامنة عبر خيوط مستقلة، ثم تعيد النتائج إلى JavaScript باستخدام `window.__nativeResult`. الإعدادات والأسرار محفوظة عبر `EncryptedSharedPreferences`، بينما المسودات والصور المحلية محفوظة على الجهاز.

الوظائف الأساسية الموجودة هي توليد المقالات عبر Article API متوافق مع OpenAI، تطبيع مخرجات النموذج والتحقق منها، توليد/اختيار الصور، مزامنة تصنيفات WordPress، فحص المقالات المنشورة وإصلاح قوالبها، النشر اليدوي إلى WordPress، Pinterest، حماية شاشة الإعدادات، وإدارة مسودات ونسخ محلية.

## نتيجة الفحص الأولي

| المجال | النتيجة |
|---|---|
| بنية المصدر | مكتملة وقابلة للتعديل، ولا يحتوي الأرشيف على أسرار حقيقية بحسب وثيقته |
| JavaScript | اجتاز `node --check` بعد استخراج كتلة `<script>` |
| HTTPS | مفروض لعناوين Article API وWordPress |
| حماية الصور | MIME فعلي من ترويسة البايت، حد 12 MB، ونسبة Pinterest exact 2:3 عند النشر |
| حماية HTML | إزالة `script/style/iframe/object/embed` ومعالجات الأحداث وروابط `javascript:` |
| WordPress | منع slug المكرر، اشتراط تصنيف موجود، والنشر لا يحدث إلا من إجراء المستخدم |
| Gradle/Android build | تعذر تشغيله في بيئة الفحص الحالية لأن `gradle` و`gradlew` وAndroid SDK غير متوفرة |

## الخلل المؤكد الذي تم إصلاحه

كانت الواجهة في `index.html` تدعم اختيار صورة اختيارية لكل وصفة في المقالات متعددة الوصفات، وترسل إلى الجسر قيمة مثل `kind=recipe-0`. لكن `NativeBridge.storeImage()` في Kotlin كان يقبل فقط `featured` أو `pinterest`، ولذلك كان كل حفظ لصورة وصفة يفشل برسالة `Unknown image type.`.

تم تعديل شرط التحقق في `MainActivity.kt` ليقبل فقط الأنواع الثلاثة التالية:

- `featured`
- `pinterest`
- `recipe-<رقم>` مثل `recipe-0` و`recipe-11`

يظل التحقق مقيدًا بتعبير منتظم، لذلك لا تم فتح قبول أنواع عشوائية أو قيم غير متوقعة. صور الوصفات لا تُعامل كصور Pinterest، وهذا صحيح لأن الواجهة تصفها كصور اختيارية ولا تفرض عليها نسبة 2:3.

## ملاحظات مخاطر/تحسينات لاحقة

1. لا توجد بيئة Android/Gradle في مساحة العمل الحالية، لذلك يلزم تشغيل `gradle :app:testDebugUnitTest` و`gradle :app:assembleRelease` على جهاز Android Studio أو بيئة تحتوي JDK 17 وSDK Platform 35 قبل تثبيت APK.
2. توجد مسارات اتصال شبكي كثيرة داخل Activity واحد؛ فصل طبقة HTTP وعمليات WordPress عن Activity سيحسن قابلية الاختبار والصيانة، لكنه تغيير معماري أكبر ولا ينبغي تنفيذه قبل تحديد النطاق.
3. `loadImage()` يعيد تحميل الصورة المحلية مع `pinterest=false`؛ هذا لا يتجاوز حاجز النشر لأن `publish()` يعيد التحقق بالصورة Pinterest مع `true`، لكنه يعني أن المعاينة قد تعرض ملفًا محفوظًا دون إعادة فحص نسبة 2:3 في تلك اللحظة.
4. اختبار الصور الاختياري لكل وصفة غير موجود كاختبار Kotlin مستقل؛ إضافة عقد اختبار لهذا النوع ستكون خطوة مناسبة بعد توفر بيئة Gradle.
5. ملفات الواجهة مضغوطة في أسطر طويلة جدًا، ما يجعل المراجعة والتعديل اليدوي أصعب؛ إعادة تنسيقها يجب أن تكون منفصلة عن تغييرات السلوك لتقليل diff غير الضروري.

## خطة التعديل المقترحة

الأولوية الآمنة التالية هي إضافة اختبار عقدي لقبول مفاتيح صور الوصفات ورفض الأنواع العشوائية، ثم تشغيل اختبارات Gradle على بيئة Android صحيحة. بعد ذلك يمكن الانتقال إلى أي مطلب وظيفي محدد مثل تحسين محرر المراجعة، تدفق Pinterest، دعم مزود Article API بعينه، أو إعادة تنظيم Activity إلى طبقات قابلة للاختبار.

## الملفات المعدلة

- `app/src/main/java/com/askinz/publisher/MainActivity.kt`

## التحقق المنفذ بعد التعديل

تم التأكد من ظهور شرط القبول الجديد في المصدر، ومن بقاء استدعاءات الواجهة متوافقة معه. كما اجتاز JavaScript فحص الصياغة. لم يتم الادعاء بنجاح بناء Android لأن أدوات البناء غير موجودة في هذه البيئة.

> لم يتم تنفيذ أي اتصال WordPress أو Pinterest أو Article API، ولم يتم نشر أو تعديل بيانات خارجية.

## نقطة القرار للمرحلة التالية

الإصلاح الأول جاهز. يمكن متابعة التعديل على أحد المسارات التالية: **مشاكل الصور والوصفات**، **توليد المقالات ومخرجات Article API**، **WordPress والنشر**، **Pinterest**، **واجهة التطبيق**، أو **إعادة الهيكلة والاختبارات**.

---

**الإصدار محل الفحص:** OrbitPress 4.0.0  
**تاريخ التحليل:** 2026-09-04

حتى هذه المرحلة لا توجد مفاتيح أو بيانات اعتماد أو بيانات WordPress حقيقية ضمن نتائج الفحص.

---

## سجل التعديل الأول

تم قبول `recipe-<رقم>` في `storeImage()` مع إبقاء التحقق الصارم من اسم النوع. هذا يصلح التناقض بين `pickRecipeImage()` في WebView و`NativeBridge` في Kotlin دون تغيير Serializer النشر أو عقد الصور المطلوبة للنشر.

## نتيجة التحقق المحلي

- فحص JavaScript: ناجح.
- فحص مصدر التعديل: ناجح.
- بناء Android واختبارات Gradle: غير منفذين لعدم وجود Gradle/Gradle Wrapper وAndroid SDK في بيئة الفحص.

## ملاحظة تسليم

هذا التقرير جزء من نسخة العمل المعدلة. يجب تشغيل اختبارات Gradle والبناء على جهاز التطوير قبل توزيع APK.

## نهاية التقرير

تم حفظ التقرير كاملًا في `ANALYSIS_AR.md`، ويُستحسن إبقاؤه مع المصدر أثناء جولات التعديل التالية.

## تفاصيل إضافية عن الامتثال

لم يتغير أي منطق متعلق بمنع التكرار، اشتراط HTTPS، JSON-LD، الحفظ المشفر، أو النشر اليدوي. التعديل محصور في قبول نوع صورة كانت الواجهة تستخدمه أصلًا.

## الخلاصة التنفيذية

المشروع منظم وظيفيًا ويحتوي على حواجز جيدة نسبيًا، لكن نقطة الصور الاختيارية للوصفات كانت غير مكتملة بين الطبقتين. أُصلحت هذه النقطة الأولى بأقل تغيير ممكن، مع توثيق حدود التحقق الحالية والاختبار المطلوب قبل الإصدار.

## حالة العمل

التحليل الأولي مكتمل، الإصلاح الأول مطبق، والتحقق الساكن مكتمل. لا توجد تعديلات أخرى على السلوك قبل تحديد أولوية المرحلة التالية.

## قائمة مراجعة قصيرة

- [x] فك ضغط المصدر.
- [x] جرد الملفات.
- [x] قراءة وثائق المشروع.
- [x] تدقيق Kotlin وWebView.
- [x] اكتشاف خلل صور الوصفات.
- [x] تطبيق إصلاح محدود.
- [x] فحص JavaScript.
- [ ] تشغيل Gradle على بيئة Android.
- [ ] إضافة اختبار Kotlin خاص بمفاتيح صور الوصفات.
- [ ] تنفيذ مجموعة التعديلات التالية حسب أولوية المستخدم.

## تنبيه حول الأرشيف

الأرشيف الناتج للاستخدام المحلي فقط، ولا يمثل APK مبنيًا أو موقعًا منشورًا. لا توجد إجراءات خارجية في هذه الجولة.

## ملاحظة أخيرة

عند طلب تعديل جديد، ينبغي تحديد السلوك المراد تغييره أو اختيار أحد المسارات المذكورة؛ أما الإصلاح الحالي فهو مستقل وقابل للمراجعة.

## توقيع التحليل

OrbitPress source review — first pass.

## نهاية

انتهى.

## ملحق: الملفات الرئيسية

- `SOURCE_README_AR.md`
- `app/src/main/java/com/askinz/publisher/MainActivity.kt`
- `app/src/main/java/com/askinz/publisher/DraftContract.kt`
- `app/src/main/java/com/askinz/publisher/LongFormCompletenessContract.kt`
- `app/src/main/java/com/askinz/publisher/ProviderCompatibilityContract.kt`
- `app/src/main/assets/index.html`

## ملحق: حدود هذه الجولة

لم يتم فتح Android Studio، ولم يتم تثبيت اعتماديات، ولم يتم استخدام بيانات اعتماد أو خدمات خارجية. جميع النتائج مبنية على قراءة المصدر وفحوص الصياغة المحلية.

## خاتمة

النسخة الحالية جاهزة للانتقال إلى الجولة الثانية من التعديل بعد مراجعة المستخدم للأولوية.
